#!/bin/sh

BASE=`dirname ${0}`
BASE=`(cd ${BASE}/..; pwd)`

export CLASSPATH=${BASE}/conf/*:${BASE}/lib/*

CLASS='com.cleversafe.oom.cli.OOMReader'

VMOPTS='-Xms64M -Xmx512M'
java ${VMOPTS} ${CLASS} "$@"
