#!/bin/sh

BASE=`dirname ${0}`
CP=${BASE}/lib/*:${BASE}/conf/log4j/og-reader/:${BASE}/conf/
CLASS='com.cleversafe.og.cli.ObjectReader'

VMOPTS='-Xms64M -Xmx512M'
java -cp ${CP} ${VMOPTS} ${CLASS} "$@"
