{
// config.sample.json is a json-formatted test configuration file for the OG
// tool. See http://www.json.org/ for more details regarding the json format.
//
// All required fields are uncommented. Each optional field is present but
// commented out, and contains a configuration line showing its respective
// default value.
//
// Note: All configuration names are strings and must be quoted e.g. "scheme"


// The httpe scheme to use. One of ("http", "https")

// "scheme": "http",


// the algorithm to use when determining which host to use for the next request.
// One of ("random", "roundrobin"). Ignored if only one host is specified. 

// "host_algorithm": "roundrobin",


// A list of hosts to use.
//
// Example1 "hosts": ["192.168.8.10"]
// Example2 "hosts": ["dc-a2100-3838.cleversafelabs.com", "10.10.1.56"]

"hosts": [],


// The http port to use.

// "port": 80,


// The object api to use. One of ("soh", "s3", "openstack", "webdav") 

// "api": "soh",


// The path at which the root of the object api resides. Object apis can be
// rooted at arbitrary base paths.
//
// Example1 "uri_root": "/"
// Example2 "uri_root": "/soh/"

// "uri_root": "/<api>/", // where <api> is the api under test


// The container to use.

"container": "",


// Request headers to set for every request.
//
// Example1 (s3 canned object acl):
// "headers": {
//	"x-amz-acl": "public-read"
// }

// "headers": {},


// Percentage of write requests.

// "write": 100.0,


// Percentage of read requests.

// "read": 0.0,


// Percentage of delete requests.

// "delete": 0.0,


// File size(s) to use for write operations. File sizes are described in terms
// of a mathematical distribution. If multiple distributions are configured, a
// distribution is randomly selected on a per-request basis based on the
// weights of each configured distribution.
//
// Distribution is one of ("uniform", "normal")
//
// Example1 A single, constant file size of 10 kilobytes.
// "filesizes": [
// {
// "average": 10,
// "average_unit": "kb"
// }
// ],

// "filesizes": [
// {
// "distribution": "uniform",
// "average": 5.0,
// "average_unit": "mebibytes",
// "spread": 0.0,
// "spread_unit": "mebibytes",
// "weight": 1
// }
// ],


// The generated source data to use for writes. One of ("random", "zeroes")

// "source": "random",


// The type of concurrency to use. One of ("iops", "threads").

// "concurrency": {
// "type": "threads",
// "count": 1
// },


// The credentials to use for the test. If username is set, password must
// also be set.
//
// "type" is one of ("basic", "awsv2").

// "authentication": {
// "type": "basic",
// "username": "",
// "password": ""
// },


// Stopping conditions that trigger the end of the test.

// "stopping_conditions": {
// "operations": 0,
// "runtime": 0.0,
// "runtime_unit": "seconds",
// "status_codes": {
// "201": 0
// },
// "aborts": 0
// },


// The root directory to store object names.

// "object_location": "./object",


// Advanced client configuration options

"client": {
	"connect_timeout": 0,
	"so_timeout": 0,
	"so_reuse_address": false,
	"so_linger": -1,
	"so_keep_alive": true,
	"tcp_no_delay": true,
	"chunked_encoding": false,
	"expect_continue": false,
	"wait_for_continue": 3000
}

}
